# Session – 2026-05-14

## Topics covered
- ORM (Object-Relational Mapping).
- Managing database changes through code.

## What I understood
- An ORM allows developers to interact with the database using classes and objects instead of writing SQL for every operation.
- ORMs generate SQL queries automatically based on the code we write.

## What is still confusing
- When it is better to use raw SQL instead of ORM abstractions.

## Questions
- How does an ORM convert objects and method calls into SQL statements?
- In what situations is raw SQL preferred over an ORM?

## Related concepts